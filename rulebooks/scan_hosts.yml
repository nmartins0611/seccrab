---
- name: Listen for events on a webhook
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:

    - name: Scan hosts for malware
      condition: event.payload.event_type is match("new-malware")
      action:
        run_template:
         name: "SC4 - Insights Update"
         organization: "Globo
